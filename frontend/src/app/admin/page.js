import AdminClient from "@/components/AdminClient";

export const metadata = {
  title: "Admin Dashboard | NextTop",
  description: "Manage your NextTop store inventory, view leads, and track revenue.",
};

export default function AdminPage() {
  return <AdminClient />;
}
